# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

*.log

# Virtual environments
.venv

# Loki Mode run state
.loki/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

.beads/interactions.jsonl
.claude/settings.json


# Emacs lock/autosave files
\#*\#
.\#*
*~

# Sandbox run output. sandbox-handoffs/ is deliberately NOT ignored: the
# handoff is the only durable account of how a bead was actually done, and the
# container that wrote it is disposable.
.sandbox.lock/
gemini_convo.md
